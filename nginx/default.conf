server {
    listen 80;
    server_name _;

    # Let's Encrypt challenge (Certbot)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Fallback: redirect to HTTPS (si ya está configurado)
    location / {
        return 301 https://$host$request_uri;
    }
}
